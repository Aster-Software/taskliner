import { action } from "@solidjs/router";
import { database } from "../../../api/src/core/database";

export const createProject = action(async () => {
    // await database.insertInto("project").values([
    //     {
    //         id: cuid
    //     }
    // ])
})